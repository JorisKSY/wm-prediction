# Raw Data

This folder contains local raw datasets used for exploration.

Raw datasets are not committed to the repository by default.

Expected local structure:

```text
data/raw/
├── international_matches.csv
├── world_cup_matches.csv
└── ...